var eingabe = prompt("Eingabe Binär");
var A = [];

function BininUni(x)
{
	for var i in 0:eingabe.size() do
	{
		var Uni = eingabe[i];
		var Bin = String.fromUnicode(Uni);
		A.push(Bin);
	}
	return A;
}

print(BininUni(eingabe));


